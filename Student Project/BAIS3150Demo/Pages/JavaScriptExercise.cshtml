@page
@model JavaScriptExerciseModel
@{
}
<!DOCTYPE html>
<html>
<head>

    <meta charset="utf-8" />
    <title>javaScriptExercise</title>
    <script type="text/javascript">
        function isValid(aForm)
        {
            var isValid = false;

            var aValue = aForm.InputField.value;
            var length = aValue.length;

            if (length > 0)
            {
                isValid = true;
            }
            else
            {
                alert('InputField is required')
            }


            var numbers = /^[0-9]$/;
            var CValue = aForm.CTextBox.value;
            var CLength = CValue.length;
            if (CLength >= 10 && CLength <= 15 && CValue.match(numbers))
            {
                isValid = true;
            }
            else
            {
                alert('Value should be digits between 10-15')
            }

            var for_email = /^w+([.-]?w+)*+([.-]?w+)*(.w{2,3})+$/;
            

            return isValid;
        }

    </script>
</head>


    <body>
        <form name="SampleForm" method="post" onsubmit="return isValid(window.document.SampleForm);" action="~/TestPost" >
            <table>
                <tr>
                    <td>
                        Name:
                    </td>
                    <td>
                        <input type="text" name="InputField" />
                    </td>
                </tr>

                <tr>
                    <td>E-Mail Address:</td>
                    <td>
                        <input type="text" name="BTextBox" id="BTextBox" />
                    </td>
                </tr>

                <tr>
                    <td>Credit Card:</td>
                    <td>
                        <input type="text" name="CTextBox" id="CTextBox" />
                    </td>
                </tr>

                <tr>
                    <td>Expiry Date:</td>
                    <td>
                        <input type="text" name="DTextBox" id="DTextBox" />
                    </td>
                </tr>

                <tr>
                    <td>
                        <input type="radio" name="ARadio" id="ARadio" value="yes" checked>Visa <br />
                        <input type="radio" name="ARadio" id="ARadio" value="no">Master Card <br />
                        <input type="radio" name="ARadio" id="ARadio" value="no">American Express <br />
                    </td>
                </tr>
                <tr>
                    <td>
                        <input type="submit" name="Submit" value="submit"  />
                    </td>
                    @*<td>
                        <input type="reset" name="Reset" value="Reset" />
                    </td>*@
                </tr>
            </table>
        </form>
    </body>
</html>